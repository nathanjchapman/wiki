# Git Flow workflow

### Features

`$ git flow feature finish`

### Releases

Start the release:

`$ git flow release start <version>` (e.g. `0.1.0`)

Bump up version number.

`$ git add CHANGELONG`
`$ git commit`

`$ git flow release finish '<version>'`

